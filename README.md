# React + Vite шаблон с TypeScript, SCSS, Router и Axios
Готовый шаблон для быстрого старта React-приложения с настроенной архитектурой, стилями и API-клиентом.

## Запуск
> `npm run dev` 	Запуск дев-сервера с горячей перезагрузкой

> `npm run build` 	Сборка для продакшена

> `npm run preview` 	Превью собранного проекта

> `npm run lint` 	 Проверка кода ESLint

### Описание проекта

📦 Стек технологий

`React 18 + TypeScript`

`Vite` - сборщик и дев-сервер

`React Router v6` - навигация

`SCSS/Sass` - препроцессор стилей

`Axios` - HTTP клиент

`ESLint` - линтер

📁 Структура проекта
```
react-vite/
├── public/
│   └── vite.svg
│
├── src/
│   ├── assets/                 # Изображения, шрифты, иконки
│   │   ├── images/
│   │   └── fonts/
│   │
│   ├── components/            # Переиспользуемые компоненты
│   │   ├── common/           # Общие компоненты (Button, Input, Card)
│   │   └── ui/              # UI компоненты (Modal, Tabs, Accordion)
│   │
│   ├── layouts/              # Компоненты макетов
│   │   └── MainLayout/
│   │       ├── components/
│   │       │   ├── Header.tsx
│   │       │   ├── Header.scss
│   │       │   ├── Footer.tsx
│   │       │   └── Footer.scss
│   │       ├── MainLayout.tsx
│   │       └── MainLayout.scss
│   │
│   ├── pages/                # Страницы приложения
│   │   ├── Home/
│   │   │   ├── Home.tsx
│   │   │   └── Home.scss
│   │   ├── About/
│   │   │   ├── About.tsx
│   │   │   └── About.scss
│   │   └── NotFound/
│   │       ├── NotFound.tsx
│   │       └── NotFound.scss
│   │
│   ├── services/            # Сервисы
│   │   └── api/
│   │       ├── controllers/ # Контроллеры API
│   │       │   ├── ...
│   │       │   ├── ...
│   │       │   └── ...
│   │       ├── config.ts   # Axios конфигурация
│   │       └── index.ts    # Экспорт API
│   │
│   ├── store/              # State management (MobX/Redux)
│   │
│   ├── styles/            # Глобальные стили
│   │   ├── base/
│   │   │   ├── _variables.scss
│   │   │   ├── _mixins.scss
│   │   │   ├── _reset.scss
│   │   │   └── _global.scss
│   │   └── main.scss
│   │
│   ├── types/             # TypeScript типы
│   │   ├── api.ts        # Типы для API
│   │   ├── user.ts       # Типы пользователей
│   │   ├── product.ts    # Типы продуктов
│   │   └── index.ts      # Экспорт типов
│   │
│   ├── utils/            # Вспомогательные функции
│   │   ├── constants.ts  # Константы
│   │   ├── helpers.ts   # Хелперы
│   │   ├── format.ts    # Форматирование
│   │   ├── validation.ts # Валидация
│   │   ├── storage.ts   # LocalStorage
│   │   └── index.ts     # Экспорт утилит
│   │
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
│
├── .env
├── .gitignore
├── index.html
├── package.json
├── package-lock.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
└── eslint.config.js
```

📋 Описание файлов
🎨 Стили и SCSS

`styles/base/_variables.scss` 	 SCSS переменные (цвета, отступы, брейкпоинты)

`styles/base/_mixins.scss` 	 SCSS миксины (медиа-запросы, flexbox, транзакции)

`styles/base/_reset.scss` 	 Сброс стандартных стилей браузера

`styles/base/_global.scss` 	 Глобальные классы (.container, .section, .text-center)

`styles/main.scss` 	 Главный SCSS файл, импортирует все части


🏗 Компоненты и Layout

`layouts/MainLayout/MainLayout.tsx` 	 Основной макет с header и footer, использует Outlet

`layouts/MainLayout/components/Header.tsx` 	 Шапка с навигационными ссылками и активными состояниями

`layouts/MainLayout/components/Footer.tsx` 	 Подвал с копирайтом

`pages/Home/Home.tsx` 	 Главная страница (шаблон)

`pages/About/About.tsx` 	 Страница "О проекте" (шаблон)

`pages/NotFound/NotFound.tsx` 	 404 страница с кнопкой возврата


🌐 API и Сервисы

`services/api/config.ts` 	 Axios конфигурация, базовый URL, таймаут, интерцепторы

`services/api/index.ts` 	 Экспорт API методов (get, post, put, patch, delete)

API клиент уже настроен:

* Базовый URL: http://localhost:3001/api

* Интерцептор запросов - добавляет токен из localStorage

* Интерцептор ответов - обрабатывает 401 ошибки

* Готов к подключению к бэкенду

⚙️ Конфигурационные файлы

`vite.config.ts` 	 Конфигурация Vite с алиасами (@/src, @components, @pages и т.д.)

`tsconfig.json` 	 Основная конфигурация TypeScript с путями для алиасов

`tsconfig.app.json` 	 Конфигурация TS для React приложения

`tsconfig.node.json` 	 Конфигурация TS для Vite и Node.js

`eslint.config.js` 	 Правила линтинга

`.env` 	 Переменные окружения (VITE_API_BASE_URL)

`.gitattributes`	 Настройки переноса строк для Windows/Linux/Mac

`index.html` 	 HTML шаблон с точкой входа

# 1. Клонировать репозиторий
git clone <repository-url>
cd react-vite-template

# 2. Установить зависимости
npm install

# 3. Запустить дев-сервер
npm run dev

# 4. Открыть в браузере
http://localhost:3000


📜 Доступные скрипты

Команда	Описание
`npm run dev` 	 Запуск дев-сервера с горячей перезагрузкой
`npm run build` 	 Сборка для продакшена
`npm run preview` 	 Превью собранного проекта
`npm run lint` 	 Проверка кода ESLint